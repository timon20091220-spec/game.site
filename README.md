# Space War / Neon Rift

HTML5 Canvas shooting game.

## GitHub Pages Upload

업로드할 파일:

- `index.html`
- `README.md`
- `.nojekyll`

GitHub Pages에서 실행하려면 저장소의 루트 폴더에 위 파일들을 올린 뒤,
Settings → Pages → Branch를 `main` / `/root`로 설정하면 됩니다.

## Main File

`index.html` 하나만으로 실행되는 단일 HTML 게임 파일입니다.
